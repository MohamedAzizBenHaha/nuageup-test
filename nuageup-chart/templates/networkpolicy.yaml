apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: nuageup-networkpolicy
spec:
  podSelector:
    matchLabels:
      app: nuageup
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          role: friend
  egress:
  - {}